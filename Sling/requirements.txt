# Sling only. To install for every game in the repo instead, use the root
# requirements.txt.
#
# The engine is not listed here on purpose: engine_bootstrap.ensure_engine()
# finds a sibling clone of visual-ai-game-engine without anything being
# installed. `pip install -e "../visual ai game engine"` (run from the repo
# root) is optional and only adds the compiled C++ physics core.
#
# An earlier version of this file ended with
#     -e "../../visual ai game engine"
# which pip resolves against the working directory rather than this file, so
# the documented `pip install -r Sling/requirements.txt` from the repo root
# failed outright.
#
# Requires Python 3.9-3.12. mediapipe publishes no wheels for 3.13+.

opencv-python>=4.8.0
numpy>=1.24.0
mediapipe>=0.10.14,<=0.10.35

# The HUD reports process CPU and memory.
psutil>=5.9.0

# Only for the pygame renderer spikes (bird_pygame.py, spike_pygame.py).
pygame>=2.5.0
